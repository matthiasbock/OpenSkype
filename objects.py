#!/usr/bin/python

class Object:
	def __init__(self, stream):
		

class ObjectList:
	def __init__(self, stream):
		self.elements = []
		self.length = ord(payload[0])
		self.extra = len(payload)-self.length

